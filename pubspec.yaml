name: Password_manager
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjust this to your current SDK version

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5 
  flutter_secure_storage: ^8.0.0 
  local_auth: ^2.1.2 
  hive: ^2.2.3 
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.3.3

flutter:
  uses-material-design: true